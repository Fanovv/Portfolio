import { cn } from "@/lib/utils";

const ProjectSection = () => {
  return (
    <section id="projects" className={cn("relative max-w-7xl mx-auto md:h-[130vh]")}></section>
  );
};

export default ProjectSection;
