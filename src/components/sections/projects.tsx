import { cn } from "@/lib/utils";

const ProjectSection = () => {
  return (
    <section id="projects" className={cn("relative max-w-7xl mx-auto md:h-[130vh] z-[2]")}></section>
  );
};

export default ProjectSection;
