import { cn } from "@/lib/utils";

const ContactSection = () => {
    return (
        <section id="contact" className={cn("relative min-h-screen max-w-7xl mx-auto")}></section>
    )
}

export default ContactSection;